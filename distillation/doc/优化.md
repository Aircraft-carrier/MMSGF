一个问题：现在 dataload 上来的数据是 RGB 还是 latent 是 latent 具体又是怎么处理的latent,

对于视频的第一帧：他对应的动作全0，对应的 geometic 一组为相同数据。


13frame 和下一个13frame 有没有一帧数重叠的

或者是 13 帧 + 16 帧这样会不会比较合理
或者是 16 帧 + 16 帧这样                

都给一个 latent 作为条件，但是这个条件也随机 mask 掉。


#
推理阶段 vggt token 是怎么计算的， x 和 g 是怎么cache 的

# 
data load 是怎么进行的。 loss mask 是怎么计算的
